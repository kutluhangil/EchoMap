/** Spacing scale. Premium minimalism leans on generous, consistent whitespace. */
export const space = { xs: 4, sm: 8, md: 12, lg: 16, xl: 24, xxl: 40, huge: 64 } as const;

/** Corner radii — soft, never sharp. */
export const radius = { sm: 8, md: 12, lg: 18, pill: 999 } as const;
